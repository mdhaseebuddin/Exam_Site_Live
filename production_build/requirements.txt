# =============================================================================
# Exam Platform — Production dependencies
# =============================================================================
# Versions below are pinned to the set verified working with this codebase.
# gunicorn is Linux/macOS-only and is what the cloud host installs; waitress
# is the Windows/local-development fallback referenced in README.md.
#
# Install with:  pip install -r requirements.txt
# =============================================================================

Flask==3.1.3
Flask-SQLAlchemy==3.1.1
Flask-WTF==1.3.0
Flask-Limiter==4.1.1
marshmallow==4.3.1
python-dotenv==1.2.2

# Brevo (formerly Sendinblue) — transactional email API for OTP/verification emails.
sib-api-v3-sdk==7.6.0

# PDF generation — pure-Python ReportLab (no system binary like wkhtmltopdf
# is required; pdfkit has intentionally been removed).
reportlab==5.0.0

# WSGI servers
gunicorn            # production server (Linux/macOS; used on the cloud host)
waitress==3.0.2     # Windows / local dev fallback

psycopg2-binary==2.9.9
